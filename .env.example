# Backend URL: GraphQL endpoint (без этого переменного фронт работает только с localStorage)
VITE_GRAPHQL_HTTP=http://localhost:3001/graphql

# Только для деплоя в подпапку (например GitHub Pages). На Render оставь пустым — сайт с корня домена.
# VITE_BASE_PATH=/HomeCookHub/
